<h1>Top 5: <%= @ranking.category.title %></h1>
<hr>
<ol>
<% @ranking.selections.each do |selection| %>
    <li><%= selection.player.name %></li>
<% end %>
    <h3>created by: <%= link_to @ranking.user.username, user_path(@ranking.user.id) %></h3>
    <%# link_to selection.player.name, player_path(selection.player) %>
    <hr>
</ol>
