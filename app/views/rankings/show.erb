<h2><%= link_to "#{@ranking.user.username}", user_path(@ranking.user.id)%>'s Top 5: <%= @ranking.category.title %></h2>
<hr>

<ol>
<% @ranking.selections.each do |selection| %>
    <li><%= selection.player.name %></li>
<% end %>
    <hr>
    <h4>Honorable Mentions:</h4>
    <p><%= @ranking.content %></p>
</ol>
